
#ifndef TESTS_
#define TESTS_

#include "atm/simple.h"

namespace tests
{
   void add_nat();
   atm::simple prop();
      // Produce a non-deterministic automaton that
      // recognizes propositional formulas.

}

#endif

